# Optional. When unset, the app targets the backend on the same hostname the
# frontend was loaded from (port 5000), so it works whether you open the app
# via localhost or 127.0.0.1. Set this only to point at a different host/port.
# VITE_API_BASE_URL="http://localhost:5000/api"
