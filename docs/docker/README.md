# Docker Docs

## Purpose
This folder documents Docker-based local development support.

## Analogy
Think of this folder as the local environment setup guide: it explains how to run supporting infrastructure consistently.

## Contents
- `DOCKER.md` documents Docker local development usage.

## How It Fits in KERNO
Docker documentation supports local setup for the services needed by the React frontend, Express backend, Prisma, and PostgreSQL workflow.

## Maintenance Notes
Update this documentation when local service names, ports, volumes, or startup commands change.
